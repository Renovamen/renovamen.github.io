---
import PostList from "@/layouts/PostList.astro";
import { getTagStaticPaths } from "@/utils";

export interface Props {
  tag: string;
}

export const getStaticPaths = () => getTagStaticPaths("en");
---

<PostList lang="en" tag={Astro.props.tag} />
