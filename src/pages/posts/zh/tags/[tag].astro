---
import PostList from "@/layouts/PostList.astro";
import { getTagStaticPaths } from "@/utils";

export interface Props {
  tag: string;
}

export const getStaticPaths = () => getTagStaticPaths("zh");
---

<PostList lang="zh" tag={Astro.props.tag} />
